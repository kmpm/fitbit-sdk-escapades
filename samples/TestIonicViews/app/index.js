import { MultiScreenApp } from './app';

console.log('App Started');

// Create and start the application.
MultiScreenApp.start();

/*
import clock from "clock";

clock.granularity = "seconds";
clock.ontick = (evt) => {
   console.log(evt.date.toTimeString());
}
*/